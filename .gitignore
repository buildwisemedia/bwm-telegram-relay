node_modules/
.wrangler/
.env
.env.*
!.env.example
*.log
.DS_Store
.bwm-ship-check-skip

# One Wire remediation: per-deploy receipts are runtime-only (plan v6 §2.11).
# Committed facts live in scripts/pinned-facts.json; deploys must never dirty the tree.
scripts/.runtime/
